@page
@model Karma.Areas.Identity.Pages.Account.OutboxModel
@{
    
}

@if (Model.Outbox.Count != 0)
{
    foreach(var message in Model.Outbox)
    {
        <div class="card mt-2">
            <div class="card-header">
                <p class="m-0">To @message.ToEmail</p>
            </div>
            <div class="card-body">
                @message.Content
            </div>
        </div>
    }
}else{
    <h1>No messages</h1>
}